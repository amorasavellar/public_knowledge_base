# Instalando poetry no ubunto

$ curl -sSL https://install.python-poetry.org | python3 -

# Iniciando poetry

$ poetry init

# Abrindo venv no poetry (Na pasta do projeto)

$ poetry shell

# Lista de ambientes poetry

$ poetry env list
